export interface highlightedProject {
  id: number;
  title: string;
  category: string;
  image: string;
  tags: string[];
}

export interface portafolioProject {
  id: number;
  title: string;
  category: string;
  image: string;
  tags: string[];
  slug: string;
}